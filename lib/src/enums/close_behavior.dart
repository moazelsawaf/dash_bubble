part of 'enums.dart';

/// Enum for the close behavior of the Bubble.
enum CloseBehavior {
  /// The close button will be moving to follow the bubble when it enters the close area.
  following,

  /// The close button will be fixed at the bottom of the screen.
  fixed,
}
